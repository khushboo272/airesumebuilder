apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: patches/replica-patch.yaml

# Dev environment uses lower resource limits and fewer replicas
# The MongoDB StatefulSet is included for local development